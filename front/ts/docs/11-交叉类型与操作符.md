# 11. 交叉类型与 `&` 操作符

本专题内容已合并到第 07 章的“6. 交叉类型与 `&` 操作符”章节，避免同一主题出现两份需要分别维护的完整文档。

请阅读：

[07-高级类型与类型运算.md：6. 交叉类型与 `&` 操作符](07-高级类型与类型运算.md#section-6)

第 07 章现在完整覆盖：

- `A & B` 的含义，以及它和联合类型 `|` 的区别。
- 组合对象能力、泛型返回值和函数类型交叉。
- 同名属性冲突、`never`、原始类型交叉和联合类型组合。
- `interface extends` 与交叉类型的选择。
- 类型交叉与运行时对象合并的区别。
- 类型位置的 `&` 与运行时按位与运算的区别。
