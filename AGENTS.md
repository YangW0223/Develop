# 项目说明

本仓库用于记录作者学习前端与后端开发的过程，包括练习项目、实验代码、学习笔记和阶段性总结。

## 目录职责

- `front/`：前端学习与实践代码。
- `back/`：后端学习与实践代码。

## 协作约定

- 优先保持示例和笔记清晰、易理解，代码应服务于学习与复盘。
- 新增内容放在对应的前后端目录中，并在必要处说明运行方式、依赖和学习目标。
- 修改已有练习时，避免无关重构；保留能体现学习过程的关键注释、提交结构和演进痕迹。
- 发现可改进之处时，兼顾正确性与可读性，并用中文解释重要的设计选择。

## 优秀学习文档的质量标准

新增教程、笔记或技术说明时，应同时满足“有广度、有深度、容易理解”三个目标。文档不应只是 API 或语法清单，而应帮助读者建立概念、完成实践，并知道何时使用或避免使用某个方案。

### 一、广度标准：形成完整知识地图

- 明确主题边界、适用场景、前置知识和学习目标。
- 覆盖从基础概念、核心语法到实际工程使用的完整路径；必要时区分“必须掌握”“建议了解”和“进阶阅读”。
- 对相关概念做横向比较，例如使用场景、优缺点、限制和与相近方案的区别。
- 说明主题与项目其他内容的关系，并链接到前置章节、后续章节或综合案例。
- 不为追求篇幅堆砌冷门 API；冷门内容应说明使用条件和学习优先级。

### 二、深度标准：解释原理和边界

- 先解释“解决什么问题、为什么需要它”，再介绍语法和 API。
- 不只给出结论，还要解释类型推导、运行机制、数据流或关键实现思路。
- 对重要示例说明输入、处理过程、输出和编译器/运行时的行为。
- 明确区分编译期与运行时、静态约束与运行时校验、约定与强制保证。
- 讲清边界条件、失败情况、常见误区、性能/安全影响以及替代方案。
- 对抽象、设计模式和高级写法，说明引入成本；优先推荐简单且可维护的方案。
- 对不能完全覆盖的内容明确说明范围，避免把近似实现或经验规则表述成绝对结论。

### 三、易懂标准：让读者可以跟着做

- 使用中文标题、统一术语和稳定的章节层级；文件名使用有顺序的中文名称，避免无意义的缩写。
- 采用由浅入深的顺序：概念直觉 → 最小语法 → 分步示例 → 常见错误 → 真实案例 → 练习总结。
- 每个小节聚焦一个问题，段落简短；首次出现的专业词汇要用通俗语言解释。
- 示例应从最小可运行版本开始，再逐步增加复杂度；每次增加内容都说明新增的知识点。
- 用表格或简短对照突出容易混淆的概念，避免只用大段文字描述。
- 错误示例使用明确的 `❌` 标记，并解释错误原因和正确改法；不要让读者误把错误代码复制运行。
- 关键代码提供注释、预期结果和运行命令；不依赖未说明的全局变量、环境或隐藏配置。

### 四、推荐文档结构

适合教程型文档的默认结构如下，可根据主题删减：

1. 标题、适用读者、前置知识和学习目标。
2. 问题背景：为什么需要这个概念，以及它解决什么问题。
3. 核心概念和 mental model（心智模型）。
4. 最小语法与逐步示例。
5. 参数、返回值、类型/数据流或执行过程说明。
6. 与相近方案的对比、适用场景和取舍。
7. 常见错误、边界条件和安全注意事项。
8. 一个贴近项目的综合案例。
9. 练习题、参考思路或验收清单。
10. 小结、延伸阅读和相关文档链接。

### 五、示例和实践标准

- 示例必须服务于当前知识点，变量名和业务背景保持一致，避免无关复杂度。
- 能运行的示例应说明依赖、目录、启动/检查命令和预期结果；无法独立运行的片段要明确标注为片段。
- 编译错误示例与正确示例分开，避免整份示例无法检查；异步、网络、浏览器 API 等环境差异要注明。
- 例子应覆盖正常路径和至少一个失败/边界路径；对外部输入要强调运行时校验，不能只依赖类型断言。
- 综合案例应复用前文概念，并展示从输入到输出的完整链路，而不是只展示零散代码。
- 练习应按难度递增；若提供答案，先给思路再给实现，避免读者跳过思考过程。

### 六、准确性、版本和可维护性

- 涉及框架、库、编译器或标准的内容，注明适用版本；可能变化的结论应链接官方文档或权威来源。
- 代码和命令应经过与文档一致的环境验证；至少检查代码围栏、路径链接、拼写和格式。
- TypeScript/代码示例应在严格配置下检查；文档中的错误示例必须使用注释或独立文件隔离。
- 修改 API、目录或文件名时，同步更新索引、交叉链接、运行说明和示例引用。
- 一个主题优先只有一个权威说明文件；重复内容应提炼为简短引用，避免多份定义逐渐不一致。
- 文档是学习记录的一部分，保留关键实验、失败原因和演进过程；改进表达时不要抹掉有助于复盘的上下文。

### 七、提交前自检

- [ ] 读者知道这篇文档适合谁、学完能做什么。
- [ ] 内容同时包含概念、原理、示例、边界和实践场景。
- [ ] 章节顺序由浅入深，术语和标题统一，文件名符合中文命名约定。
- [ ] 正确示例可运行或已明确说明运行条件，错误示例不会误导复制。
- [ ] 重要结论有验证依据，版本敏感内容有来源和版本说明。
- [ ] 已更新目录和相关链接，并通过格式、链接和代码检查。
